package realWorldAPI.objects;

import java.util.UUID;

public class Object {
    /**
     *         UUID uuid = UUID.randomUUID();
     *         String uuidAsString = uuid.toString();
     *         UUID sameUuid = UUID.fromString(uuidAsString);
     *         assert sameUuid.equals(uuid);
     */
    public UUID objectID;
}
